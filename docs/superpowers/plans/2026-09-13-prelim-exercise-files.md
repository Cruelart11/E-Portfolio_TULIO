# Prelim Exercise Files Implementation Plan

1. Copy the three supplied Exercise 1 documents into a stable public portfolio directory using web-safe filenames.
2. Extract the supplied worksheet values into static React data without changing the original workbook.
3. Replace the Prelim placeholder statistics, term label, activities, and folder data with the approved real coursework sections.
4. Add reusable document-preview markup for the two embedded PDFs and the scrollable spreadsheet table, including original-file links and PDF fallbacks.
5. Add responsive styling for the document cards, viewers, availability notices, and bounded spreadsheet scrolling.
6. Verify document paths, displayed content, removed placeholders, lint, production build, and desktop/mobile rendering.
